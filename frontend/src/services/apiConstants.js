import { API_URL } from "../config";

export const LOGIN = `${API_URL}/login`
export const REGISTER = `${API_URL}/register`
export const LEADERBOARD  = `${API_URL}/leaderboard`
export const UPDATE_SCORES = `${API_URL}/updatescore`;